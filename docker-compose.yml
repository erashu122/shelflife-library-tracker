services:
  mongo:
    image: mongo:7
    container_name: shelflife-mongo
    environment:
      MONGO_INITDB_DATABASE: shelflife
    ports:
      - "27017:27017"
    volumes:
      - mongo-data:/data/db

volumes:
  mongo-data:
